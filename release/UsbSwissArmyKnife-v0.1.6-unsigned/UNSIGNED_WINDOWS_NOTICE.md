# Unsigned Windows Notice

This pre-release EXE is unsigned. Windows SmartScreen warnings are expected.

Only run builds downloaded from the official project release page and verify the published SHA-256 checksum before testing.
