# USB Swiss Army Knife v0.1.6 - PowerShell XML Escape Fix

This release fixes a PowerShell parser error in the optional XLSX workbook writer.

## Fixed

- Replaced invalid PowerShell string escaping in the worksheet XML generator.
- Keeps the console hold/debug behavior from v0.1.5 so runtime errors remain visible.
- Keeps the guided build flow, Everything purpose, editable CSV/XLSX planning output, and source URL planning fields.

## Boundary

This tool does not download, bundle, format, erase, install, or redistribute third-party tools. It creates structure, templates, editable build plans, source records, and local documentation only.
