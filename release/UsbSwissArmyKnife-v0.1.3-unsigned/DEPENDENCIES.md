# Dependencies

Runtime:

- Python standard library only inside the bundled launcher.
- Windows PowerShell available on the target Windows system.

Build-time:

- PyInstaller, used to package the console launcher as an EXE.

No third-party runtime tools, binaries, installers, ISOs, archives, wordlists, VM images, vendor tools, or downloaded project content are redistributed.
